# Add custom aliases here if need, but prefer writing them as scripts in ../scripts
alias readit="bat --style=plain,header readme.md"
alias gp="git push"
alias gc="git-commit-edit"
alias gca="git-add-commit"
alias ga="git add ."
alias gsw="git-switch-pick"
alias gswc="git switch --create"
